# SWOT River Dynamics Dashboard - Dependencies (Streamlit Cloud)
# For the full ingestion pipeline (SWOT_Pull.py), use: pip install -r requirements-full.txt

# Dashboard framework
streamlit>=1.49.0   # >=1.49 for the width="stretch" API (used throughout); also covers on_select
plotly>=5.17.0

# Data & computation
duckdb>=1.0.0
pandas>=2.0.0
numpy>=1.24.0
scipy>=1.10.0
pyarrow>=14.0.0           # pandas parquet engine for the DEM cross-section artifacts (also a Streamlit dep)

# Maps
folium>=0.15.0
streamlit-folium>=0.15.0
matplotlib>=3.7.0         # Used for colormaps only
