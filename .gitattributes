# Git LFS Configuration for SWOT Dashboard
# Track large parquet files using Git Large File Storage

# Optimized dashboard data file (single file, ~23MB, 1.9M rows)
batch_outputs/dashboard_data_optimized.parquet filter=lfs diff=lfs merge=lfs -text

# Note: This file contains last 6 months of data (sampled 50%) for fast dashboard performance.
# Reduces from 6.7M rows → 1.9M rows for Streamlit Cloud's 1GB RAM limit.
# Full dataset remains local only (gitignored).
