# Thesis & Figure Impact Log

Running record of code fixes whose effects must later be propagated into the
thesis text and regenerated figures (SWOT + DEM). Maintained during the
post-review fix campaign (Aug 2026); worked through as its own project once the
code and analysis are settled. Findings reference `docs/CODE_REVIEW_FINDINGS.md`.

| # | Date | Fix (finding) | What changes downstream | Thesis touchpoints |
|---|------|---------------|-------------------------|--------------------|
| 1 | 2026-08-14 | Granule-keyed checkpoints (SWOT_Pull.py:585/:185, critical+high) | Archive re-pull will recover 169 never-ingested sibling-tile granules; ~27% of archive dates are currently 4-km stubs (pass 571 missing ~85% of profile on 50 dates) and become full profiles. Pass/date counts, coverage statistics, and every figure or table built from the master change after re-ingestion. Reference gradient was protected by the ≥8-node gate (stub dates contributed 0 rows) but its gated-pass counts (n=88/67) will grow. | §2.2 data inventory / pass counts; any figure caption citing number of passes or observations; temporal-analysis sample sizes. |
| 2 | 2026-08-14 | MAD filter moved from raw WSE to 1-km node-median residuals (SWOT_Pull.py:320, critical) | Ends the upstream-Uyak amputation (46 dates, ~28%). Validated on stored 2023-09-01: Uyak removal 24.6%→1.1% with nodes 13–22 km fully restored; Kanektok now catches real 2–4 m artifacts the raw band (±72.8 m) missed. After re-pull, published Uyak reference gradient (191.7 cm/km) will shift UP toward ~192.0 (verifier: 6 amputated gated passes at median 184.2 dragged it down), slightly narrowing the Kanektok–Uyak gradient gap; direction of conclusion unchanged. | §2.3: change "detrended residuals" to node-median residuals (code now matches intent); §3.x headline gradients (195.4 vs 191.7) and every derived number; Fig/table with per-pass slope distributions. |
| 3 | 2026-08-14 | Ice-season hard line May–Oct (`ICE_SAFE_MONTHS`, new — user-requested ice filtering) | Master shrinks 188 → 90 dates (7.9M → 3.4M rows); winter/shoulder data stays in daily CSVs only. Reference gradient (gated) becomes Kanektok 195.3 (n=64), Uyak 192.0 (n=54) on current archive — Uyak rises to exactly the verifier's "clean pass" value because contaminated April passes (incl. 2025-04-19, a breakup pass the manual registry missed) drop out. Gap narrows 3.7 → 3.3 cm/km, direction unchanged. Deployed dashboard data and temporal analysis lose all Nov–Apr dates. | §2.x methods: replace "Apr–Nov open-water window" with May–Oct hard line + the empirical calibration (April breakup every year, October clean, first freeze-up mid-Nov); headline gradients & n counts everywhere; temporal-analysis season definitions; any figure showing April or November data. |
| 4 | 2026-08-14 | Removed superseded per-granule pixel-OLS `slope_calc` export (SWOT_Pull.py:346, low) + drift-proofing: `rebuild_master.py` now delegates to the single pipeline implementation, QC registry single-sourced in `qc_registry.py` | Master/daily-CSV schema loses the `slope_calc` column (density-biased, never displayed, superseded by Theil–Sen refgrad). `gradient_prototype.py` (the historical method-comparison script) will no longer run against new masters — its conclusions are already recorded. | If the thesis anywhere cites the old pooled/per-pass OLS numbers as "the dashboard's gradient," remove/replace; methods §: single QC registry + wrapper rebuild are worth a sentence in reproducibility. |
| 5 | 2026-08-14 | Fine-scale gap-fill no longer fabricates WSE in gaps wider than 0.3 km (dashboard_swot.py + thesis_figures/core.py `_fine_regular_grid`, high) | ~3,984 previously-fabricated 0.1-km grid cells (of ~87k) across the archive become honest NaN gaps; the Fig 9 fine-scale slope profile changes shape at sparse-coverage bins (verifier: up to 124 cm/km locally); headline near-bifurcation medians essentially unchanged (258.9→258.0 in verification). Applied identically in dashboard and thesis-figure module. | Fig 9 must be regenerated; §2.5.x gap-fill description ("gaps ≤0.3 km interpolated") is now actually true as written — no text change needed beyond regenerating numbers; any fine-scale profile values quoted in §3. |
| 6 | 2026-08-14 | **ARCHIVE RE-PULLED & REBUILT** (209 May–Oct granules; 120 with river pixels; all ingestion fixes active) | **NEW CANONICAL REFERENCE GRADIENT: Kanektok 195.3 cm/km (n=93, std 0.4); Uyak 192.4 cm/km (n=95, std 3.0); advantage 2.9 cm/km** (was 195.4/191.7, gap 3.6, n=88/67). Verified in new master: 95 dates / 4.84M rows, May–Oct only, classes [3,4], zero amputated Uyak dates (was 46), all 25 pass-571 dates full-span (was 0), no multi-pass dates. Old daily CSVs archived in `batch_outputs/data_legacy_prefix_20260814/`. Deployed `dashboard_data.parquet` on GitHub Release is still the OLD data until redeploy (entry pending). | EVERY quoted gradient, n, advantage, and derived number in §3 and abstract; all SWOT figures need regeneration from the new master; §2.2 inventory (95 analysis dates, 120 granules). |
| 7 | 2026-08-14 | Analysis-layer batch: Slope Profile tab made gap-aware (NaN-holes + NaN-aware Gaussian, was bridging holes with up to ~280 cm/km spurious slope); "Slope at Coast/Anchor" labels un-swapped (index 0 IS the anchor); temporal tab Upper/Lower river labels corrected at source (temporal_analysis.py had upstream/downstream inverted — small dist = inland anchor = upper river); fine-scale IQR band now abs-then-quantile (same-sign bins unchanged); cache-busting now keyed on data fingerprint (ETag/mtime) instead of the constant URL | Slope Profile tab curves show honest gaps; stats table columns now geographically correct (values themselves were always right — only labels lied); temporal Q3 JSON must be REGENERATED before the dashboard redeploy (stored JSON has old inverted field semantics AND old-archive data); redeployed data now picked up within 1 h automatically. | If any thesis table quotes "slope at coast/anchor" from the dashboard stats table, re-check which number went where; Q3 profile up/downstream statements in §3.x must be re-checked after temporal rerun. |
| 8 | 2026-08-14 | DEM batch: geoid hull fallback → nearest-neighbour extrapolation (DEM_Pull.py, medium — the 13.46 m constant put ~0.25 m datum error into 36,803 out-of-hull points, concentrated at the Uyak mouth); arc example capture fixed from banker's-rounding to exact radii (build_arc_B.py — arcB_sections.png panels were the R−0.5 km arcs labeled R); DEM-stage superelevation fallback removed (verified no-op on committed data); beta reproduction chain committed to repo (reference/avulsion_transects.gpkg + reference/original_beta.parquet + recover_original_beta.py; end-to-end rerun reproduces recovered original β 0.96/H_AR 4.30 m and naive-rebuild β 2.86/99%) | `dem_river_elevations.parquet` must be REGENERATED (run DEM_Pull.py extraction) to pick up the geoid fix — until then the DEM profile's terminal Uyak bin is ~0.24 m too low (artificial mouth dip) and the anchor bins ~0.33 m high; arcB_sections.png regenerates with correct labels on next build_arc_B.py run; arc parquets and all headline superelevation/β numbers UNCHANGED | DEM profile figures (D-series) and any DEM-vs-SWOT residual discussion over coastal Uyak (32–36 km); figure caption for arc cross-sections if it names the radii; §2.x DEM datum methods sentence (interpolated + NN-extrapolated, not constant-fallback). |
| 9 | 2026-08-14 | Temporal stats upgrade + RERUN ON NEW ARCHIVE (temporal_analysis.py findings 17/18): significance now family-wise (Holm step-down over all 16 Mann–Whitney tests, exported as p_*_holm; dashboard stars adjusted p); Q3 vs-baseline verdict now a bootstrap 95% CI (n=10000, seed=42) on \|storm dWSE\|−\|baseline dWSE\| with a three-way exceeds/within/indistinguishable outcome; open-water window single-sourced from qc_registry (May–Oct) | **NEW TEMPORAL RESULTS (188 gated passes, 2023-07-31→2026-08-10):** the contested Uyak seasonal-slope effect is GONE on the recovered archive (raw p=0.150, was 0.033) — the thesis's "Not Significant" label is now right for two independent reasons, but its quoted p must change; Q1 seasonal WSE: only Uyak-2024 (−0.44 m) survives Holm; Q2 stays significant after Holm (Kanektok slope +0.5 cm/km p≈0.0000 — tiny but consistent; WSE −0.23/−0.36 m, drier 2025); Q3 both rivers INDISTINGUISHABLE from baseline (excess CIs [−0.27,+0.09] / [−0.39,+0.27] m span 0). temporal_results/ JSON+parquets regenerated — the task-15 "regenerate temporal before redeploy" prerequisite is DONE. Record now contains Jul–Aug 2026 passes (n=7 through Aug 10): the DEFINITIVE Q3 Jul–Aug comparison becomes runnable after the season completes (~Sep 2026 pull) | Seasonal-slope table: replace p=0.033 with the new p=0.150 (and state the Holm rule in methods §2.x); Q3 wording everywhere: "within natural baseline" → "statistically indistinguishable from natural variability (bootstrap CI)"; TEMPORAL_ANALYSIS.md carries a superseded-numbers banner until its rewrite; any quoted temporal n's (188 passes). |
| 10 | 2026-08-14 | **dem_river_elevations.parquet REGENERATED** (DEM_Pull.py with the geoid fix, current polygons, geoid interpolator rebuilt from the NEW granule-keyed CSVs) | Two changes landed together: (1) geoid fix verified — anchor-bin shift −0.007 m matches the verifier's predicted value exactly; only 306 mouth pixels remain out-of-hull (was ~84% of the terminal bin) because the recovered sibling tiles extended SWOT coverage, and those 306 now get the true 13.202 m by NN extrapolation, so the ~0.24 m artificial mouth dip mechanism is gone. (2) Discovery: the old parquet (Jun 29) PREDATED the Jul 13 polygon edit (c84d4c8) — it was stale against the committed polygons all along. New extraction: 2,401,447 points (was 2,485,204); mid-river bins (2–30 km) essentially unchanged (median \|Δ\|=0.0004 m); terminal Uyak bin now n=2793, median 1.49 m (was n=5228, 1.74 m — composition change from the polygon refresh, not datum). Old parquet preserved in scratchpad this session only. | DEM profile figures and any quoted DEM mouth-bin elevations; §2.x data inventory if it quotes DEM point counts; the deployed dem_river_elevations.parquet on GitHub Release v2.0-data is now BOTH datum-wrong and polygon-stale until redeploy. |
| 11 | 2026-08-14 | **ALL THESIS FIGURES REGENERATED** on the fixed pipeline + new archive (SWOT Figs 1–9 via `python3 -m thesis_figures.make_figures --all`; DEM Figs 1A–D/2/5 via `make_dem_figures 1 2 5`) and the DEPLOYMENT PARQUET rebuilt (`dashboard_data.parquet`: 2.56M rows, 50 dates 2025-05-08→2026-08-10, 81 MB, all passes ≥2025-04-01 from the new master) | Every figure now reflects: granule-recovered coverage, residual MAD, May–Oct ice line, gap-honest fine-scale/slope pipelines, abs-then-quantile bands, fixed DEM geoid + current polygons. NOT YET DEPLOYED. **Hard ordering constraint verified: the live main-branch dashboard queries `slope_calc` (dashboard_swot.py:1155 on main), which the new parquet no longer carries — the code merge to main MUST precede the release-asset swap or the live app crashes.** Assets to upload after merge: dashboard_data.parquet, dem_river_elevations.parquet, reference_gradient_per_pass.parquet (stable filenames, `gh release upload --clobber`). | Thesis figure files themselves (swap into the document); captions quoting n/dates (50 deployed dates, archive 95); README/dev-notes deploy recipe still shows the stale Apr–Jul month filter — update when deploying. |
| 12 | 2026-08-16 | **Hygiene sweep** (docs/HYGIENE_TRIAGE.md; 58 unverified Phase-B findings triaged with the owner, ~40 fixed) | Three corrections carry thesis weight: (1) **"confluence" terminology retired repo-wide** — the 0 km point is the ANCHOR POINT, ~2.5 km upriver of the bifurcation; the rivers split, they don't meet (README, SCIENTIFIC_METHODOLOGY, code comments all corrected). (2) **The "~0.34 m" DEM differential-stage artifact restated as ~0.27 m** (per-arc median of DEM-diff minus SWOT-paired-diff, n=64 arcs, recomputed from committed arcB_channels.parquet — the 0.34 figure predates the current artifact and reproduces from nothing). (3) **Spherical-distance convention documented**: all dist_km/radii use great-circle geometry, understating ellipsoidal ground distance ~0.36% (uniform across rivers/epochs; comparisons unaffected; never switch piecemeal). Also: study extent is ~36 km (not ~70 km); dashboard fine-scale n_passes now counts only passes that contributed data (reliability gates may fire on selections that previously passed). | §1/§2 wherever "confluence" appears — replace with anchor point (~2.5 km upriver of the bifurcation); any quoted differential-stage artifact (0.34 → 0.27 m); §2.x distance-methods paragraph gains the spherical-convention sentence; any "~70 km" reach-extent statement → ~36 km. |
