"""Thesis figure generation for the Kanektok avulsion-risk thesis.

Separate, print-first figure module. The science comes from the shared
`swot_core` package (the same implementation the dashboards run; `core.py` is
a thin façade over it), combined with publication styling in `config.py`.
See make_figures.py for the CLI.
"""
